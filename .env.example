# Optional contact-form override. The application currently has a public fallback form ID.
VITE_FORMSPREE_ID=

# Server-only review queue configuration. Never expose these as VITE_* values.
SANITY_PROJECT_ID=h3pl1rfx
SANITY_DATASET=production
SANITY_WRITE_TOKEN=
PORTFOLIO_INGESTION_TOKEN=
AUTHORITY_REVIEW_TOKEN=
AUTHORITY_PUBLICATION_TOKEN=

# Durable Authority -> Portal -> PRIME event relay.
PORTFOLIO_RELAY_URL=https://the-portal-ten.vercel.app/api/portfolio-relay
PORTFOLIO_RELAY_TOKEN=

# Optional server-side override for the public-safe JARVIS trace relay.
# The production code has a safe default and requires no secret to read it.
JARVIS_PUBLIC_TRACE_URL=https://bksyjvppcwfgwoelnyvp.supabase.co/functions/v1/jarvis-trace-relay
